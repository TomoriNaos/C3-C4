# 运动控制参数
motion_controller_node:
  ros__parameters:
    # 控制循环参数
    control_hz: 50.0              # 控制频率（Hz）

    # 任务与到点判定参数
    arrive_radius_m: 2.0          # 到达半径（m）
    hover_altitude_m: 20.0        # 默认悬停高度,无人机不会低于此高度飞行（m）

    # 回船目标由主控持续发布到 /mission/goal，运动模块不再维护 home_x/y/z 参数
